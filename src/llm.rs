use crate::database::Screenshot;
use color_eyre::Result;

pub async fn generate_description(screenshot: &Screenshot) -> Result<String> {
    use tokio::fs;

    // let screenshot_bytes =
    Ok("placeholder".into())
}
